rules_version = '2';

// App chỉ truy cập Firestore từ server (Admin SDK) — chặn toàn bộ truy cập trực tiếp từ trình duyệt.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
